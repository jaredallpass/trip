<%= form_with(model: video_sub_category, local: true) do |form| %>
  <% if video_sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_sub_category.errors.count, "error") %> prohibited this video_sub_category from being saved:</h2>

      <ul>
      <% video_sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
