<%= form_with(model: image_sub_category, local: true) do |form| %>
  <% if image_sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_sub_category.errors.count, "error") %> prohibited this image_sub_category from being saved:</h2>

      <ul>
      <% image_sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
