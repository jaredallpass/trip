<%= form_with(model: image_category, local: true) do |form| %>
  <% if image_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_category.errors.count, "error") %> prohibited this image_category from being saved:</h2>

      <ul>
      <% image_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
